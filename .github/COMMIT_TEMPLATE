# <type>(<scope>): <subject>
# |<----  限制 50 个字符以内  ---->|
#
# 详细说明（Body，可选）：
#   - 解释本次提交改动了什么（What）
#   - 解释为什么这样改（Why）
#   - 与之前版本的对比（可选）
#   - 每行不超过 72 个字符
#
# 页脚（Footer，可选）：
#   - 关联 Issue: Refs #123, Fixes #456
#   - 破坏性变更: BREAKING CHANGE: xxx
#   - 共同作者: Co-authored-by: Name <email>
#
# --------------------
# Type 类型（必填其一）：
#   feat     新功能
#   fix      Bug 修复
#   docs     文档变更
#   style    格式（不影响代码运行）
#   refactor 重构（既非新功能也非 bug 修复）
#   perf     性能优化
#   test     增加/修改测试
#   chore    构建工具、依赖、辅助工具变更
#   ci       CI/CD 配置变更
#   build    构建系统变更
#   revert   回退之前的 commit
#
# Scope 范围（可选）：
#   backend / frontend / docs / .github / root
#
# Subject 主题（必填）：
#   - 使用祈使句，现在时："add" 而非 "added" / "adds"
#   - 首字母不大写
#   - 结尾不加句号
#   - 简洁明确
#
# 完整示例：
#   feat(backend): add sse streaming for convert task
#
#   - 新增 GET /api/convert/{task_id}/stream 端点
#   - 使用 sse-starlette 推送实时进度
#   - 兼容老版本 polling 接口
#
#   Refs #12
# --------------------
